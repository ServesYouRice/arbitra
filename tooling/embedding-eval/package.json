{
  "name": "arbitra-embedding-eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "P18 local embedding clustering evaluation runner. Not a workspace package: the model runtime stays out of every runtime dependency graph.",
  "scripts": {
    "fetch-model": "tsx run.ts --fetch-only",
    "eval": "tsx run.ts"
  },
  "devDependencies": {
    "@huggingface/transformers": "4.3.0",
    "tsx": "4.23.15"
  }
}
