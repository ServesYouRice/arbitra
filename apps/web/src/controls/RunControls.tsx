import { useEffect, useRef, useState, type ReactElement } from "react";
import { RunApi, type RunResource } from "../api/runs.js";
export function RunControls({ api, configurationId, initialRunId = null }: { readonly api: RunApi; readonly configurationId: string; readonly initialRunId?: string | null }): ReactElement { const [repository, setRepository] = useState(""); const [run, setRun] = useState<RunResource | null>(null); const [estimate, setEstimate] = useState<unknown>(null); const [error, setError] = useState<string | null>(null); const invoke = async (operation: () => Promise<RunResource>): Promise<void> => { try { setRun(await operation()); setError(null); } catch (cause) { setError(cause instanceof Error ? cause.message : String(cause)); } }; useEffect(() => { if (initialRunId !== null) void invoke(() => api.status(initialRunId)); }, [api, initialRunId]); const checkpoint = run?.checkpoints[0]; return <section aria-labelledby="run-controls-title"><h2 className="panel-title" id="run-controls-title">run controls</h2><label>repository<input value={repository} onChange={(event) => setRepository(event.target.value)} /></label><div className="run-actions"><button type="button" onClick={async () => { try { await api.selectRepository(repository); setError(null); } catch (cause) { setError(cause instanceof Error ? cause.message : String(cause)); } }}>select</button><button type="button" onClick={async () => { try { setEstimate(await api.estimate(configurationId, repository)); setError(null); } catch (cause) { setError(cause instanceof Error ? cause.message : String(cause)); } }}>estimate</button><button type="button" onClick={() => { void invoke(() => api.start(configurationId, repository)); }}>start</button>{run === null ? null : <><button type="button" onClick={() => { void invoke(() => api.status(run.runId)); }}>status</button><button type="button" onClick={() => { void invoke(() => api.resume(run.runId)); }}>resume</button><button type="button" onClick={() => { void invoke(() => api.cancel(run.runId)); }}>cancel</button></>}</div>{estimate === null ? null : <pre aria-label="run estimate">{JSON.stringify(estimate, null, 2)}</pre>}{run === null ? <p className="state" data-state="unexamined">run unavailable</p> : <p>run {run.runId} · {run.state} · {run.resumable ? "resumable" : "not resumable"}</p>}{checkpoint === undefined ? null : <div className="checkpoint" role="alert"><p>{checkpoint.stage} · {checkpoint.prompt}</p><button type="button" onClick={async () => { await api.respondCheckpoint(run!.runId, checkpoint.id, "continue"); await invoke(() => api.status(run!.runId)); }}>continue</button><button type="button" onClick={async () => { await api.respondCheckpoint(run!.runId, checkpoint.id, "cancel"); await invoke(() => api.status(run!.runId)); }}>cancel at checkpoint</button></div>}{error === null ? null : <p className="state" data-state="degraded" role="alert">{error}</p>}</section>; }
export function InspectorOverlay({ open, title, onDismiss, children }: { readonly open: boolean; readonly title: string; readonly onDismiss: () => void; readonly children: ReactElement }): ReactElement | null { const close = useRef<HTMLButtonElement>(null); useEffect(() => { if (!open) return; close.current?.focus(); const onKey = (event: KeyboardEvent): void => { if (event.key === "Escape") onDismiss(); }; document.addEventListener("keydown", onKey); return () => document.removeEventListener("keydown", onKey); }, [open, onDismiss]); if (!open) return null; return <aside aria-label={title} aria-modal="true" className="inspector-overlay" role="dialog"><button ref={close} type="button" onClick={onDismiss}>close inspector</button>{children}</aside>; }

