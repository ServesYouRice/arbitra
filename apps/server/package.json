{
  "name": "@arbitra/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/serve.js",
    "dev": "node dist/src/serve.js"
  },
  "dependencies": {
    "@arbitra/schemas": "workspace:*",
    "fastify": "5.12.1",
    "@arbitra/runtime": "workspace:*"
  }
}
